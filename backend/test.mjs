import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function test() { try { const p = await prisma.product.create({ data: { displayId: Math.floor(Math.random()*10000), name: 'Teste',   price: 10,   storeId: 'cm7jopj3u0000aayz1p1b8w20', sku: null  }}); console.log('ok', p); } catch(e) { console.error('Erro:', e) } finally { await prisma.$disconnect() } }; test();